[package]
name = "read-ahead-bench"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
smb2 = { path = "../../crates/smb2", features = ["__bench-tuning"] }
tokio = { version = "1", features = ["rt-multi-thread", "macros", "time"] }

# Standalone: this sits under the main clone when checked out as a worktree.
[workspace]
